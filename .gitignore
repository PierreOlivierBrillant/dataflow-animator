# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-demo
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/
.gemini/
.codegraph/
.mcp.json

coverage/

# Angular CLI build cache (packages/angular, written by `ng test`)
.angular/

# Playwright (harness visual regression) — the goldens (*-snapshots/) ARE
# committed; the run reports and diffs are not.
test-results/
playwright-report/
blob-report/

# A/B gate scratch results (*.ab.spec.ts): written by the tests, read and
# printed by globalTeardown, one run at a time.
**/scripts/validation-harness/.ab-results/